{% include 'Header' %}

import UIKit
import Combine

protocol {{ module_info.name }}Delegate: AnyObject {

}

final class {{ module_info.name }}ViewModel: ObservableObject {
    private weak var delegate: {{ module_info.name }}Delegate? 

    init(delegate: {{ module_info.name }}Delegate?) {
        self.delegate = delegate
    }
}

extension {{ module_info.name }}ViewModel {
}
